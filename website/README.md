# Getting start

## running development
1. install dependencies `npm install`
2. edit you `.env` file
3. run development server `npm start`

## deploy to firebase
1. login with firebase cli `firebase login`
2. init firebase if u didn't `firebase init`
3. build webapp into build folder `npm run build`
4. deploy to firebase hosting `firebase deploy`